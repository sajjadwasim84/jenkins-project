pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh '''
                    echo 'this is the first pipeline project ..'
                    ls -lah
                    pwd
                '''

            }
        }

    }
